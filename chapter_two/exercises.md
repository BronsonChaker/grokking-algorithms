### Exercises

**2.1** If there alot of inserts and very few reads a Linked List would be preferable as insertion is BigO(n)

**2.2** Linked List to implement this queue as insertions and deletions are BigO(1) for linked this as we store the head and t ial fo the linked list. Arrays would require moving all nums in list for insertion anmd or deletion

**2.3** In this instant for binary search I would implement an array

**2.4** Downsides for an array on inserts is that the system will ne to allocate blocks of memory big enough to store the additonal memory so this can fail, also insertion is BigO(n). Inserts can fial if not enough ememory to allocate contiguously.

**2.5** Arrays, hybrid data structure is faster for insertion and relative for searching. For Linked Lists hybrid model is same/relative for insertion and simnilar for searching
